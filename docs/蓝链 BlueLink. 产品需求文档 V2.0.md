**版本**：2.0
**日期**：2026年6月5日
**团队**：Eau
**产品定位**：端侧AI认知溯源引擎——不生产答案，只提供直达真相的最短捷径。

---

## V2.0 修订说明

V2.0 在 V1.0 产品逻辑完整保留的基础上,基于 **Android 原生（Kotlin + Jetpack Compose）** 技术栈重写所有技术相关章节。V1.0 的产品愿景、用户分析、核心模块、功能需求、交互原则、设计语言、术语表等业务内容 **未做改动**。本版本主要差异：

| 章节 | V1.0 现状 | V2.0 调整 |
|------|----------|----------|
| 7.1 AI 模型协同 | 要求"端侧 LLaMA 3 8B 量化版"等 | 端侧 LLM 改为**"未来可扩展位"**，当前 MVP **全部走云端 API** |
| 7.2 数据存储 | Chroma Mobile / Qdrant 描述 | 增加 **Room + Tink + Android Keystore** 端侧存储说明 |
| 7.3 关键算法 | 纯算法描述 | 补充 Android 端实现指引（OCR、STT、SimHash 计算位置） |
| 8.1 性能 | 仅端到端延迟 | 增加 **冷启动、内存、续航、APK 体积** 等 Android 原生指标 |
| 8.4 兼容性 | iOS 16+ / Android 13+ | 删除 iOS，改为 **Android 7.0 ~ Android 15+ 完整矩阵** |
| 8.5 可扩展性 | 描述较泛 | 明确 **KMP / Compose Multiplatform** 作为 iOS 未来预留方向 |

> 本版本 **不覆盖** 的内容（沿用 V1.0）：1~6 章产品逻辑、附录术语表、附录示意图说明。

---

## 1. 产品概述
### 1.1 产品愿景
蓝链（BlueLink.）是一款面向深度学习者与内容创作者的端侧AI认知溯源引擎。它的目标是实现AI从"答案生成器"到知识的索引与溯源工具的转变,强迫并引导用户回归原始语境,进行主动思考与知识内化。产品通过"溯源降噪"过滤重复信息,运用"引力线"将灵感直连原文,构建个性化知识图谱,最终让用户以最短路径抵达真相源头。
### 1.2 核心价值主张
+ **AI不生产答案，只提供索引与溯源**：打破"虚假获得感"，强迫用户回归原文阅读。
+ **知识不出端，隐私即自由**：敏感数据本地处理，构建绝对安全的个人"第二大脑"。
+ **增量降噪，只给干货**：自动识别并折叠重复内容，仅展现真正的"信息增量"。
+ **费曼伴学，自我校准**：通过比对用户表述与原文逻辑，定位认知偏差，引导主动内化。
+ **个性化溯源粒度**：用户可自由调控提示明确度、探索深度与溯源颗粒度。
### 1.3 目标场景
+ 高校科研工作者进行文献研读、综述撰写、理论溯源。
+ 内容创作者深度阅读材料，提炼观点并记录灵感。
+ 终身学习者构建个人知识体系，抵御信息过载与AI依赖。

### 1.4 产品差异化
| 对比维度 | 蓝链 BlueLink    | 竞品（Obsidian/Flomo/闪念贝壳） |
| ---- | -------------- | ----------------------- |
| AI角色 | 索引与溯源，不直接给答案   | 生成、总结或被动存储              |
| 知识处理 | 实时去重、增量折叠、原文锚点 | 手动链接、简单标签或仅速记           |
| 隐私安全 | 敏感数据端侧本地处理     | 多数依赖云端                  |
| 交互范式 | 锚点穿梭、引力线、聚光灯阅读 | 传统气泡或单一列表               |
| 学习理论 | 内置费曼学习法偏差提示    | 无内置学习校验                 |

## 2. 用户分析
### 2.1 目标用户画像
+ **核心受众**：具有中高强度深度学习需求的群体，如高校科研人员、硕士/博士生、内容创作者、知识型博主。
+ **年龄区间**：20-40岁。
+ **行为特征**：
    - 长期进行资料研读、论文阅读与撰写。
    - 需要频繁记录并整理灵感碎片。
    - 担心AI导致思维惰性，希望保持独立思考能力。
    - 对数据隐私较为敏感，不愿将未成形想法或敏感文档上传云端。
### 2.2 核心痛点
1. **思维惰性与虚假获得感**：依赖AI总结，误以为已掌握知识，削弱独立推演能力。
2. **信息重复噪音**：海量文档中大量背景、常识性内容重复，消耗筛选精力。
3. **隐私焦虑**：云端AI服务无法保证个人灵感与敏感资料的绝对安全，导致用户不敢真实表达。
4. **知识孤岛**：不同来源的笔记、灵感与文献彼此割裂，难以发现深层关联。

## 3. 产品架构与核心逻辑
蓝链整体分为四大核心模块，覆盖从知识摄入、处理、内化到关联的全流程：

```plain
[灵感/文档导入] → [知识库（去重、切片）] → [对话溯源（锚点/费曼伴学）]
                         ↕
[阅读器（聚光灯/折叠）] ← → [知识图谱（引力线连接）]
```

**核心闭环流程**：捕获 → 入库降噪 → 溯源提问 → 锚点跳转原文 → 聚光灯阅读 → 灵感笔记关联 → 图谱生长。
**AI协作模式（V2.0 修订）**：
- **当前 MVP**：所有 AI 推理（含隐私文档处理）**统一走云端 API（FastAPI 后端代理 Moonshot/DeepSeek）**。客户端只做调度、缓存、脱敏。
- **未来可扩展**：客户端预留本地模型推理接口位（`OnDeviceLLMProvider`），未来可下沉到 MediaPipe LLM Inference / llama.cpp Android。**V2.0 不实现端侧 LLM 部署**。
- 敏感数据划分由用户手动标记及 AI 自动识别共同完成。

## 4. 功能需求详述
### 4.1 知识库与文档管理
**模块定位**：用户所有文献、灵感、多模态数据的统一管理中心，具备智能去重与提示能力。

| 功能点              | 详细描述                                                              | 优先级 |
| ---------------- | ----------------------------------------------------------------- | --- |
| 多格式导入            | 支持PDF、Word、Markdown、HTML、纯文本、图片OCR、语音转文字、网页剪藏。                    | P0  |
| 入库去重与增量折叠        | 导入新文档时，AI比对已有知识库，自动识别重复概念与已知背景，将其折叠。只向用户高亮展示"信息增量"内容（如新观点、反常识数据）。 | P0  |
| 概念路标（代替原有的自动化摘要） | 提取文章试图回答的核心问题或争议点（例如："本文探讨了XX与YY的关系，并对传统ZZ理论提出了质疑"）               | P0  |
| 原文切片与索引          | 文档解析后按段落、句子语义切片存储，建立向量索引，支持精准定位任何概念所在原文片段。                        | P0  |
| 灵感快速捕获           | 首页悬浮快捷栏支持文字速记、语音录入、拍照/扫描多模态输入，一键生成灵感卡片并入知识库。                      | P0  |
| 手动标记与分类          | 用户可自定义标签、文件夹，标记文档为"私密/敏感"，敏感文档即锁定端侧处理。                            | P1  |
| 版本与来源追溯          | 保留文档原始来源与修改记录，每条灵感与原文的关系可回看。                                      | P1  |

**交互细节**：文库首页采用瀑布流卡片布局，每张卡片显示标题、摘要、来源、时间戳。卡片附带轻微阴影与圆角，长按可呼出更多操作。

### 4.2 对话与溯源引擎
**模块定位**：用户进行概念提问、费曼学习的核心界面，强调"只给路径，不给答案"。

| 功能点 | 详细描述 | 优先级 |
| --- | --- | --- |
| 概念提问与锚点生成 | 用户输入疑问，AI在知识库中检索，返回一段引导引言及若干"锚点卡片"，卡片内容为原文切片的关键句及定位信息。 | P0 |
| 锚点跳转与聚光灯联动 | 点击锚点卡片，自动跳转至文档内对应段落，触发聚光灯模式（见4.3阅读器）。 | P0 |
| 提问历史与线索保留 | 每一次提问生成的引言和锚点集合可作为线索卡片保留在对话流中，支持回看。 | P1 |
| 溯源粒度动态调节 | 全局或临时切换：文章级粒度（溯源至篇章/小节）、句词级粒度（定位到具体句子或术语）。 | P0 |
| 提示明确度调节 | 设置档位：直接指出逻辑漏洞并给出原文对比；或仅抛出矛盾的原文段落，由用户自行发现错误。 | P1 |
| 探索深度控制 | 标准：仅回答当前疑问；深度探索：AI在提供溯源后主动推荐关联度高的争议性文献或对立的观点。 | P1 |
| 对话中断与恢复 | 支持随时跳出阅读，返回后对话状态保留。 | P2 |

**交互规则**：对话界面弱化传统气泡，信息呈现在卡片中。AI的响应不使用长文本，仅提供引言（最多三句话）和锚点列表。输入框底部常驻"溯源粒度"快捷切换开关。

### 4.3 智能阅读器
**模块定位**：提供两类阅读模式，分别服务于溯源跳转与自主浏览，核心在于"降噪"与"聚焦"。
#### 4.3.1 聚光灯模式（由锚点触发）
+ **触发条件**：从对话锚点跳转进入。
+ **表现**：目标段落维持正常对比度（黑字白底），其余上下文降低透明度至30%并呈现灰态，但文字仍可阅读（不缺失）。
+ **退出**：点击屏幕任意空白处，聚光灯消散，平滑过渡为全文正常显示。
+ **附加**：在目标段落旁显示一条垂直的"引力线"指示器，标注跳转来源。
#### 4.3.2 增量折叠模式（由文库主动打开）
+ **触发条件**：从文库卡片直接打开文档。
+ **表现**：文档完整保留，但AI识别出的重复背景、已知概念段落被"内联折叠"，显示为一条极简折叠提示条："AI已折叠 XXX 字重复背景，点击展开"。
+ **高亮**：未被折叠的"信息增量"段落边缘可有淡蓝色左侧边框标记。
+ **交互**：点击折叠条可展开，再次点击收起。
#### 4.3.3 通用阅读功能：
+ 支持目录导航、全文搜索。
+ 阅读进度保存与书签。
+ 支持从阅读界面快速摘录并生成灵感卡片（长按选中文字 → 发送至灵感库）。

### 4.4 知识图谱
**模块定位**：将用户文档、灵感碎片、概念实体编织成一张动态星图，揭示隐性关联。

| 功能点 | 详细描述 | 优先级 |
| --- | --- | --- |
| 节点自动生成 | AI从文档与灵感中抽取实体、概念作为节点，灵感和文档本身也为节点。 | P0 |
| 引力线连接 | AI自动发现节点间的相似、引用、矛盾、补充等关系，绘制连线并标注关系类型（如"支撑"、"质疑"）。 | P0 |
| 无极缩放与局部聚焦 | 图谱支持手势缩放；点击节点，镜头拉近并居中，高亮该节点及一阶强关联节点，其余淡化为背景。 | P0 |
| 节点类型区分 | 灵感节点、文献节点、概念节点采用不同形状或颜色标识。 | P1 |
| 手动增删连线 | 用户可拖拽建立新关系或删除错误关系，AI据此优化模型。 | P1 |
| 溯源路径回放 | 选中两个节点，可展示它们之间的最短关联路径，突出关键节点。 | P2 |

### 4.5 费曼伴学系统
**模块定位**：基于费曼学习法的主动知识校验工具，迫使学习者用自己的话解释概念，AI仅负责匹配原文并指出偏差。

| 功能点        | 详细描述                                                        | 优先级 |
| ---------- | ----------------------------------------------------------- | --- |
| 费曼挑战启动     | 用户在对话界面选择"费曼模式"，AI提示用户就某概念进行用自己的语言复述解释。                     | P0  |
| 实时语音/文字输入  | 支持语音输入（端侧转文字）或键盘输入解释内容。                                     | P0  |
| 偏差识别与引力线绑定 | AI在本地知识库中比对用户表述与原文，识别逻辑漏洞、事实错误或遗漏关键点；将错误部分与对应原文片段建立"引力线"连接。 | P0  |
| 偏差报告生成     | 生成通俗的纠偏报告，不直接显示正确表述，而是高亮偏差处并附原文定位锚点。                        | P0  |
| 多轮追踪       | 用户根据锚点阅读原文后，可再次解释，系统持续校准并记录理解进步。                            | P1  |

### 4.6 灵感捕获与聚合
**模块定位**：记录一切碎片想法，后台自动进行语义关联与图谱集成。
+ **快捷入口**：首页底部悬浮 **一个 FAB**（克莱因蓝圆形"+号"），点击展开 **三 Tab 弹层**（`ModalBottomSheet`），分别提供文字速记、语音、拍照/扫描三种录入方式。详见 UI&UX 文档 V2.0 §4.6。
+ **灵感卡片**：每条灵感生成独立卡片，AI自动提取关键词并打标签。卡片随文库瀑布流展示。
+ **后台关联**：灵感保存后，系统异步分析其内容，自动搜寻已有知识库及旧想法中的关联点，生成引力线，并在图谱中呈现。
+ **灵感演变**：允许将多张卡片合并、关联，逐步形成文章大纲或论文框架。
### 4.7 隐私与端侧处理
+ **敏感数据识别**：用户可手动将文档/灵感标记为"私密"；AI也可根据内容自动建议标记。
+ **端侧模型覆盖（V2.0 修订）**：MVP 阶段所有 AI 调用走云端。**V2.0 通过"客户端脱敏 + 云端 API + 端侧加密存储"的组合实现隐私保护**：
    - 客户端对私密文档/灵感的原文不离开设备，仅将脱敏后的关键词、片段 ID、向量特征上传。
    - 客户端本地数据库使用 **Tink + Android Keystore** 进行 AES-256 全量加密。
    - 预留 `OnDeviceLLMProvider` 接口，未来可下沉端侧 LLM。
+ **云端协同边界**：非敏感大篇幅文档的向量化、全局去重比对可调用云端算力，但原始文本可根据用户设置决定是否上传（默认不上传，仅上传脱敏的向量特征）。
+ **本地加密存储**：知识库整体加密存储在设备本地，并可同步到用户私有云（WebDAV 等，**iCloud 暂不支持**），传输过程加密。
+ **权限与隐私声明**：首次使用明确说明数据处理方式，提供详细隐私设置。
### 4.8 认知分层与个性化
+ **用户画像构建**：初次使用引导填写职业、专业领域、兴趣、价值观关键词。系统也在使用中动态学习用户的知识背景与常用概念层级。
+ **语境适配**：AI在生成引言、线索时，优先采用用户画像中的术语体系。例如对物理系学生使用"哈密顿量"而非"能量函数"。
+ **伴读风格切换**：提供"温和启发"与"直指核心"等多种语气，或根据当前任务紧急度自动调整。
+ **知识盲区感知**：当提问中涉及用户尚未入库的概念时，AI可提示"这一概念暂未在你的知识库中找到，是否联网搜索或手动添加？"（联网功能可配置开关）。

## 5. 交互设计规范
### 5.1 核心交互原则
1. **拒绝投喂，只给路径**：任何AI响应都不得直接给出最终答案，必须通过锚点或引力线引导用户返回原文。
2. **一触即达**：缩短从产生疑问到查看原文的步长，点击锚点立即跳转。
3. **空间方位感**：通过磨砂玻璃、渐进式界面，让用户始终清楚自己所处位置。
4. **动态调控权**：溯源粒度、提示明确度等控制项始终触手可及。
### 5.2 锚点穿梭机制详解
+ 在对话界面，AI返回的每个锚点卡片包含：文档标题、来源片段前20字、跳转按钮。
+ 点击锚点，文档阅读器以模态或全屏方式展开，并自动滚动至目标段落，聚光灯亮起。
+ 用户验证后，可通过左滑手势或关闭按钮返回对话，对话历史保留该锚点已读状态。
### 5.3 手势与快捷操作
+ 双击阅读页面空白区 → 退出聚光灯/展开所有折叠。
+ 长按锚点卡片 → 预览原文小窗（Peek）。
+ 在灵感输入框下滑 → 切换到语音输入。
+ 图谱中双指缩放，单指拖动，双击节点聚焦。
### 5.4 界面流转逻辑
+ **提问 → 溯源 → 阅读 → 返回**：
    1. 用户在对话视图输入问题。
    2. AI生成引言与锚点卡片列表。
    3. 用户点击某锚点，界面从对话平滑过渡到阅读器（聚光灯模式）。
    4. 用户点击空白处或返回按钮，阅读器滑出，对话界面重现。
+ **灵感捕获 → 卡片生成 → 图谱更新**：
    1. 用户在任何界面呼出快捷栏，录入灵感。
    2. 卡片自动追加到文库瀑布流顶部。
    3. 后台处理完成后，图谱中出现新节点，并动态生长引力线，可提示用户查看。

## 6. 视觉设计规范
### 6.1 设计哲学
"人文底色，极简透视"。平衡古典纸本阅读的静谧与现代AI工具的锐利，让机器隐匿于文字之后。

### 6.2 色彩体系
+ **背景主色**：米白色 (#FAF7F2) / 羊皮纸暖色，模拟纸质阅读感。
+ **强调色**：克莱因蓝 (#002FA7)，用于所有超链接、按钮、引力线、高亮边线、锚点卡片边框。
+ **文字色**：主文本 (#2C2C2C)，次要文本 (#6B6B6B)。
+ **折叠/灰态**：灰色 (#B0B0B0) 透明度30%应用于聚光灯下的非焦点文本。
+ **磨砂玻璃**：白色半透明配合高斯模糊值约20px，用于悬浮栏、半屏卡片背景。
### 6.3 字体规范
+ **阅读/内容区域**：采用古典衬线体（如中文使用宋体/明体衍生，西文使用 Garamond 或 Caslon），字号基准16pt，行间距1.8倍。
+ **UI控件/标题**：使用无衬线体（如中文苹方/思源黑体，西文SF Pro Display或Inter），保持现代感。
### 6.4 形状与材质
+ 卡片：大圆角（16px），轻量弥散阴影（Y偏移2px，模糊半径8px，透明度10%）。
+ 按钮与输入框：圆角24px。
+ 悬浮元素：高斯模糊背景，边缘半透。
+ 图标：线性图标，描边2px，颜色统一采用中性灰或克莱因蓝。
### 6.5 布局原则
+ 大量留白，界面元素间距宽松，减少视觉压迫。
+ 核心界面对话、阅读、文库、图谱各自保持独立空间，通过底部导航栏切换。
+ 底部导航图标+文字，当前选中项使用克莱因蓝强调。

## 7. 技术实现要求
### 7.1 AI 模型协同架构（V2.0 重写）

#### 7.1.1 MVP 阶段（V2.0 当前）
- **云端大模型（唯一通道）**：
    - 任务：文档解析、向量嵌入、跨文档去重、锚点生成、费曼评估、图谱关系发现、摘要。
    - 选型：Moonshot (Kimi) / DeepSeek，通过自建 FastAPI 后端代理调用。
    - 客户端只负责构造请求、解析响应、缓存结果，**不直接持有 API Key**。

#### 7.1.2 端侧能力（V2.0 范围）
| 能力 | 选型 | 说明 |
|------|------|------|
| OCR（文字识别） | **ML Kit Text Recognition** | Google 端侧 OCR，免费、离线、英文+简中支持好 |
| 语音转文字 | **Android SpeechRecognizer** | 系统 API，依赖设备厂商实现，需权限申请 |
| 文档向量 | **云端 API 计算** | 客户端仅接收 768/1024 维向量并本地存储 |
| 费曼偏差比对 | **云端 API** | 调用大模型，客户端展示结果 |
| 端侧 LLM | **❌ 不实现** | 预留 `OnDeviceLLMProvider` 接口，未来接入 MediaPipe LLM Inference / llama.cpp Android |

#### 7.1.3 端云数据流原则（V2.0 重写）
- 用户标记为"私密"的文档：其全文、原文切片、向量**均不出设备**。客户端仅上传脱敏的关键词、片段 ID；云端无法访问原文。
- 非私密文档：向量可上传云端增强检索，原文默认**不上传**，仅在用户显式同意后才上传。
- 同步策略：支持 WebDAV 端到端加密同步（**iCloud 同步预留位但 V2.0 不实现**）。

### 7.2 数据存储与向量数据库（V2.0 重写）
- **客户端主数据库**：**Room**（SQLite 之上的 ORM 抽象），存储文档、切片、灵感卡片、锚点缓存、图谱节点/边。
- **客户端加密层**：**Tink + Android Keystore** 管理主密钥，对 Room 中的 `text_encrypted` / `vector_blob` 字段做 AES-256-GCM 加密。
- **偏好设置**：**DataStore (Preferences)** 存用户画像、粒度偏好、隐私开关。
- **向量检索**：MVP 阶段使用 **Room + 简单余弦相似度**（适合 < 1 万切片规模）。若超 1 万切片或召回质量不达标，可升级至 **Chroma Embedded Android** 或 **FAISS-Android**。
- **云端向量库（可选）**：Qdrant / Milvus（仅存非私密向量）。

### 7.3 关键算法与实现（V2.0 重写）
- **文档解析与切片**：客户端不解析（避免 APK 膨胀与兼容性坑），**统一交由 FastAPI 后端解析**（PyMuPDF / python-docx / markdown）。客户端通过 `GET /documents/{id}/segments` 获取结构化 JSON。
- **OCR**：客户端调用 **ML Kit Text Recognition**，识别后作为新文档或灵感卡片入库。
- **STT**：客户端调用 **SpeechRecognizer** + 端侧语言包，识别后作为灵感文本。
- **去重与增量检测**：客户端仅持有已分段的加密向量，**相似度计算放后端**（SimHash + 语义向量余弦相似度，详见详细设计文档 V2.0 §9.2）。
- **实体抽取与图谱构建**：客户端只负责展示，数据由后端 `GraphBuilder` 服务通过大模型或 NER 抽取后推送。
- **费曼偏差检测**：用户解释文本走 `/api/v1/feynman/evaluate`，后端调用大模型做 NLI 式比对，客户端接收 `DeviationResult` 并渲染。

### 7.4 端云数据流控制（V2.0 重写）
- 用户标记为"私密"的文档，其全文和向量绝对停留在本地。
- 非私密文档的向量可上传云端增强检索，原文根据用户设置决定（默认不上传）。
- 同步策略：支持 **WebDAV** 端到端加密同步知识库和设置（**iCloud 暂不支持**）。

## 8. 非功能需求（V2.0 重写）
### 8.1 性能
| 指标 | 目标值 | 测量方法 |
|------|--------|----------|
| 冷启动到首页可交互 | < 2.0s（中端机如骁龙 778G） | Android Profiler Macrobenchmark |
| 锚点跳转文档定位 | < 500ms | Trace `bringIntoViewRequester` + Room 查询 |
| 文库列表（1000 卡片）滚动 | 稳定 60fps | Compose Layout Inspector / Profile GPU Rendering |
| 知识图谱渲染（500 节点 + 边） | 首次 < 1.5s，交互 60fps | WebView ECharts `ready` 事件 + JS 性能 API |
| 本地去重（1MB 文档） | < 3s（含网络） | 后端耗时 + 客户端渲染总时长 |
| 内存占用（前台空闲） | < 200MB | Android Studio Profiler Memory |
| APK 体积 | < 30MB（不含扩展包） | `./gradlew :app:bundleRelease` |
| 后台耗电 | < 5% / 小时（无前台操作） | Battery Historian |

### 8.2 安全与隐私
+ 本地数据库全量加密（**AES-256-GCM via Tink + Android Keystore**）。
+ 主密钥派生：用户首次启动生成，**永不离开 Keystore**。
+ 传输过程使用 TLS 1.3。
+ 严格遵守《个人信息保护法》，提供数据导出与删除功能。
+ 应用集成 **Google Play Data Safety Form** 所需的所有声明字段。

### 8.3 可用性
+ 首次使用引导不超过3屏，关键功能有直观教程。
+ 核心任务（提问-溯源-阅读）完成步数不超过3步。
+ 支持无障碍化：TalkBack、字体缩放、对比度切换、动态取色（Material3 You）。
+ 适配 **Material Design 3** 动态主题（Android 12+ Monet）。

### 8.4 兼容性（V2.0 重写）
| 维度 | 范围 |
|------|------|
| minSdk | **24**（Android 7.0 Nougat），覆盖率 > 97% |
| targetSdk | **36**（Android 16） |
| compileSdk | **36** |
| 屏幕 | 手机为主（sw320dp ~ sw480dp），后续可扩展平板 |
| 架构 | arm64-v8a（必须）、armeabi-v7a（可选）、x86_64（模拟器） |
| 厂商 | 重点测试 **小米（MIUI/HyperOS）、华为（HarmonyOS）、OPPO（ColorOS）、vivo（OriginOS）、三星（OneUI）** 的后台限制与权限弹窗行为 |
| 系统功能依赖 | ML Kit（Google Play Services）、SpeechRecognizer（系统自带）、WebView（≥ 80） |
| 不支持 | **iOS、Web、桌面端**（详见 §8.5） |

### 8.5 可扩展性（V2.0 重写）
+ **Android 端扩展**：
    - 插件化 AI Provider：未来可接入 ChatGLM、文心一言等，封装为统一 `LLMProvider` 接口。
    - 预留 `OnDeviceLLMProvider` 接口，待端侧 LLM 成熟（量化 1-3B 模型 + 设备内存 > 8GB）时实现。
    - 插件化文档解析：客户端可后续按需下载特定格式解析器（动态功能模块 `Dynamic Feature`）。
+ **跨平台预留**：
    - 业务逻辑层（UseCase、Repository、ViewModel）使用 **Kotlin 纯 JVM 代码**，未来可通过 **Kotlin Multiplatform (KMP)** 共享到 iOS。
    - UI 层（Compose）未来可通过 **Compose Multiplatform** 迁移到 iOS / 桌面。
    - **V2.0 不实现 KMP 模块化**，但代码组织上保证 `domain/` 与 `data/` 层零 Android 依赖。

## 9. 附录
### 9.1 术语表
+ **锚点（Anchor）**：AI从原文提取的定位线索，可点击跳转到具体段落。
+ **引力线**：代表概念、文档、灵感之间逻辑关系的可视化连线。
+ **聚光灯模式**：阅读器中高亮目标段落、弱化其余内容的显示状态。
+ **增量折叠**：将用户已熟悉的重复内容自动收起，仅显示新信息。
+ **溯源颗粒度**：AI返回线索的定位精度级别，如文章级或句词级。
+ **费曼伴学**：引导用户用自己的语言解释概念并与原文比对的学习模式。

### 9.2 参考界面示意图说明
+ 首页：顶部搜索栏，下方瀑布卡片流，底部悬浮快捷栏（+号展开语音、文字、拍照）。
+ 对话界面：消息以卡片流呈现，每条AI回复带引言和锚点列表，底部有粒度切换开关。
+ 阅读界面：聚光灯模式下，中部高亮文本，上下方半透明灰态；折叠模式下，折叠条显示淡蓝色虚线。
+ 图谱界面：黑色/深灰背景，节点与连线呈现，底部有导航和聚焦控制。

---

**文档变更记录**

| 版本 | 日期 | 修订内容 | 作者 |
| --- | --- | --- | --- |
| 1.0 | 2026-05-26 | 初始版本，覆盖全部核心需求 | Eau团队 |
| 2.0 | 2026-06-05 | 基于 Android Kotlin + Jetpack Compose 栈重写技术章节；保留 V1.0 全部产品逻辑；删除 iOS 端侧 LLM 要求 | Eau团队 |
